
# ── Core ──────────────────────────────────────────────────────────────────────
# Local dev uses opencv-python (with GUI); Docker uses opencv-python-headless.
# Uncomment the correct line for your environment:
opencv-python==4.13.0.92
# opencv-python-headless==4.13.0.92
numpy==2.4.2
scipy==1.17.1
PyYAML==6.0.3
pillow==12.1.1
matplotlib==3.10.8
psutil==7.2.2
pandas==2.3.3
pydantic==2.12.5

# ── ONNX Runtime (replaces PyTorch — ~50 MB instead of ~2 GB) ────────────────
onnxruntime==1.24.2
onnx==1.20.1
onnxslim==0.1.86
# ── API ───────────────────────────────────────────────────────────────────────
fastapi==0.135.1
uvicorn[standard]==0.41.0
# ── OCR ───────────────────────────────────────────────────────────────────────
paddleocr>=2.7.0
paddlepaddle>=2.6.0
