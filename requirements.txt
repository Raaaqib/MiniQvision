# Raaqib NVR — Dependencies
# NOTE: ultralytics is installed with --no-deps in Dockerfile
#       to avoid pulling in PyTorch (~2GB). Only ONNX Runtime is needed.

# Core
opencv-python-headless>=4.9.0
numpy>=1.24.0
scipy>=1.11.0
pyyaml>=6.0
pillow>=9.0.0
matplotlib>=3.3.0
psutil>=5.8.0
pandas>=1.4.0
tqdm>=4.64.0

# ONNX Runtime (replaces PyTorch — ~50MB instead of ~2GB)
onnxruntime>=1.17.0
onnx>=1.15.0
onnxslim>=0.1.71

# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Dashboard
streamlit>=1.35.0
requests>=2.31.0

# MQTT (optional)
# paho-mqtt>=2.0.0

# Edge TPU (optional — Coral USB)
# pycoral (install separately: https://coral.ai/docs/accelerator/get-started/)
